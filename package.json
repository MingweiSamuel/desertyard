{
  "scripts": {
    "dev": "wrangler dev --test-scheduled",
    "serve": "wrangler pages dev www",
    "build": "wrangler build",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.5.1"
  }
}